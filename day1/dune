(executable
 (name bin)
 (modules bin)
 (libraries lib))

(library
 (modules lib fixture)
 (name lib)
 (libraries containers))

; (tests
;  (names test)
;  (modules test)
;  (libraries alcotest lib))

; (alias
;  (name runtest)
;  (deps test.exe))
